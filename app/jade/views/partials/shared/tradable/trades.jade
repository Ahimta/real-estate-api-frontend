mixin trades
  +my-pending('!trades')
    section.col-sm-3.list-group
      a.list-group-item(ng-repeat='trade in trades',
        ng-click='select(trade.id)',ng-bind='trade.name',
        ng-class='{active: isSelected(trade.id)}')
